name: path_provider_linux
description: Local stub package for Android-only builds.
version: 2.2.1
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: PathProviderLinux